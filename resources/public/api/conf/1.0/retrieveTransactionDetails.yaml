get:
  summary: Retrieve a Self Assessment Transaction's Detail [test only]
  description: |
    This endpoint allows a developer to retrieve more detail about a specific transaction. A unique identifier must be used for both the account (National Insurance number) and the transaction (transactionId).
    
    ### Test data
    Scenario simulation using Gov-Test-Scenario headers is only available in the sandbox environment.
    
    | Header Value (Gov-Test-Scenario)      | Scenario                                                                                  |
    |---------------------------------------|-------------------------------------------------------------------------------------------|
    | N/A - DEFAULT                         | Simulates a successful response with a self-assessment accounts.                          |
    | PAYMENT_ON_ACCOUNT                    | Simulates a successful response with a payment.                                           |
    | NOT_FOUND                             | Simulates the scenario where no data is found.                                            |
    | NO_DETAILS_FOUND                      | Simulates the scenario where no transaction details can be found for this transaction ID. |     

  tags:
    - Payments and Liabilities

  parameters:
    - $ref: './common/pathParameters.yaml#/components/parameters/nino'
    - $ref: './common/pathParameters.yaml#/components/parameters/transactionId'
    - $ref: './common/headers.yaml#/components/parameters/acceptJson'
    - $ref: './common/headers.yaml#/components/parameters/authorizationReadScopeHeader'
    - $ref: './common/headers.yaml#/components/parameters/testScenarioHeader'

  security:
    - User-Restricted:
        - read:self-assessment

  responses:
    "200":
      description: Success
      headers:
        X-CorrelationId:
          $ref: './common/headers.yaml#/components/parameters/correlationId'
      content:
        application/json:
          schema:
            $ref: './schemas/retrieve_transaction_details_response.json'
          examples:
            Payment example:
              description: Payment example
              value:
                $ref: './examples/retrieveTransactionDetails/retrieve_payment_response.json'
            Charge example:
              description: Charge example
              value:
                $ref: './examples/retrieveTransactionDetails/retrieve_charge_response.json'


    "400":
      description: Bad request
      content:
        application/json:
          schema:
            $ref: './schemas/error-response.yaml'
          examples:
            FORMAT_NINO:
              $ref: './common/errors.yaml#/components/examples/formatNino'
            FORMAT_TRANSACTION_ID:
              $ref: './common/errors.yaml#/components/examples/formatTransactionId'


    "403":
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: './schemas/error-response.yaml'
          examples:
            CLIENT_OR_AGENT_NOT_AUTHORISED:
              $ref: './common/errors.yaml#/components/examples/clientOrAgentNotAuthorised'

    "404":
      description: Not Found
      content:
        application/json:
          schema:
            $ref: './schemas/error-response.yaml'
          examples:
            MATCHING_RESOURCE_NOT_FOUND:
              $ref: './common/errors.yaml#/components/examples/notFound'
            NO_DETAILS_FOUND:
              $ref: './common/errors.yaml#/components/examples/notFoundTransactionDetails'